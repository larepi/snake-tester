# This file was edited by CloudBees Feature Management
type: experiment
flag: snakeGame.release
value: false




# Add the SDK (rox) to your application
npm install rox-browser --save

# For TypeScript projects, also add the types
npm install @types/rox-browser --save
